// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://flobell.github.io/mi-portafolio',
  base: '/portfolio/', // <- Muy importante para GitHub Pages
});